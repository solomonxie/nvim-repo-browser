// T2.1 (see docs/design/repo-browser-plan.md) -- classify a file as
// text/image/binary/too-large. Ports the buildFileNode logic from the old
// src/indexer/walk.ts (pre-pivot) into a per-request live read.

export {};
