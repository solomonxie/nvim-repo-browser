# React app shell

The SPA the live server serves — file tree, breadcrumb, Code/Commits/Insights
tabs, markdown/mermaid/syntax highlighting, all rendered client-side from the
server's JSON API. Built once via `npm run build` into `../dist-shell/`.
